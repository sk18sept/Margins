# Ignore everything except the file you want to commit
# Put '*' here if you want to ignore everything except the files mentioned below...
# Then, use the negation pattern to exclude the file from being ignored
git_auto_push.py
git_auto_push.log
